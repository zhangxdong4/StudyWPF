<Page
    x:Class="StudyWPF.Pages.Curricula"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:StudyWPF.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Curricula"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Page.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Foreground" Value="Red" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FF458077" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <!--  这里显示课程信息  -->
            <StackPanel>
                <TextBlock
                    Margin="10"
                    FontSize="20"
                    Text="课程列表" />
                <ListBox Margin="10" Background="Transparent">

                    <ListBox.Template>
                        <ControlTemplate TargetType="ListBox">
                            <Border
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                SnapsToDevicePixels="true">
                                <ScrollViewer Margin="0" Focusable="false">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </ListBox.Template>

                    <ListBoxItem Margin="10" Content="添加课程" />
                    <ListBoxItem Margin="10" Content="删除课程" />
                    <ListBoxItem Margin="10" Content="修改课程" />


                </ListBox>
            </StackPanel>
        </Grid>
        <Grid Grid.Column="1">
            <!--  那里显示详细内容  -->
            <Frame
                x:Name="DetailFrame"
                Margin="10"
                Background="Transparent"
                BorderThickness="0"
                NavigationUIVisibility="Hidden"
                Source="AddCurricula.xaml"/>
        </Grid>

    </Grid>
</Page>
